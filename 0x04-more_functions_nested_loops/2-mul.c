#include "main.h"
/**
 * mul - a function that multiplies two integers.
 * @a : digit for multiplication
 * @b : digit for multiplication
 * Return: multiplication of a & b
 */
int mul(int a, int b)
{
	return (a * b);
}
