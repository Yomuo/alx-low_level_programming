#include "main.h"
/**
 * add - function that adds two integers and returns the result.
 * Return: return result
 * @a : number to add
 * @b : number to add
 */
int add(int a, int b)
{
	result = a + b;
	return (result);
}
