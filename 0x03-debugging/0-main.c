#include "main.h"
/**
 * main - function are testing the positive_or_negative()
 * Return: return 0 if gives the correct output
 */
int main(void)
{
int n;

n = 0;
positive_or_negative(n);

return (0);
}
